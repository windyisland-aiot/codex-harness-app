[package]
name = "harness-appserver"
version = "0.1.0"
edition = "2021"
description = "codex app-server stdio JSON-RPC 客户端（T05）"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
