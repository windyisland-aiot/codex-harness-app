[package]
name = "harness-config"
version = "0.1.0"
edition = "2021"
description = "codex config.toml 读写（T09 配置面板）"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
thiserror = "1"