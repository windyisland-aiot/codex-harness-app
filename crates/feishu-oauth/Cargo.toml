[package]
name = "harness-feishu-oauth"
version = "0.1.0"
edition = "2021"
description = "T13 飞书 OAuth：app_access_token / user_access_token 获取与自动刷新"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }
