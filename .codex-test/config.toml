# 本地端到端验证用配置：指向 mock Responses API
model = "mock-model"
model_provider = "mock"
approval_policy = "never"

[model_providers.mock]
name = "mock"
base_url = "http://127.0.0.1:8791/v1"
env_key = "MOCK_KEY"
wire_api = "responses"